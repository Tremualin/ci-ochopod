cluster:  hook
image:    paugamo/test-hook
debug:    true
ports:
    - 10000

verbatim:
  cpus: 1.0
  mem:  128

  #
  # - don't forget to place this pod on a public slave so that Jenkins can reach it
  #
  acceptedResourceRoles:
    - slave_public
